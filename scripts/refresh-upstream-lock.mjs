import { execFileSync } from "node:child_process";

throw new Error(
  "Automated promotion is intentionally split into the private operations repository; run its signed candidate pipeline instead of editing the public lock in place",
);
